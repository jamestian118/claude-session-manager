# CHANGELOG

All notable changes to this project will be documented in this file.

## 0.1.0 - 2026-02-27

### Added
- Test framework and coverage gate (`>=60%`) with CLI/TUI/provider coverage.
- CI workflow (`py_compile`, `ruff`, `pytest`).
- CLI enhancements: colored output, `list --limit/--json`, clearer help defaults.

### Changed
- Command routing and return-code normalization (`0/1/2`).
- MCP handoff snapshot resolution correctness and path parameterization.
- AGENTS defaults expanded with repo map, coding standards, and testing requirements.
